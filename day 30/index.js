var sam = "sameer";
let age = 25;
const city = "mumbai";

console.log(sam, age, city)
console.table([sam, age, city])

sam = "vaiS"
console.log(sam);

age = 23
console.log(age);

city = "pune"
console.log(city);

/**
 * 
1. var: Function-scoped or globally-scoped variable (older way)
2. let: Block-scoped variable, can be reassigned
3. const: Block-scoped variable, cannot be reassigned
 * 
 */
